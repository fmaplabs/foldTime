node_modules
.DS_Store
dist
dist-ssr
*.local
.env
# Cloudflare local Worker secrets — never commit real values (keep the example).
.dev.vars
.dev.vars.*
!.dev.vars.example
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
# Regenerated by `wrangler types` (pnpm cf-typegen); env-dependent.
worker-configuration.d.ts
